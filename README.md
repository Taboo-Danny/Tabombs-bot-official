## HOW TO SETUP AND USE BOT FOR YOURSELF

1. Clone this repo
2. Install dependencies `pip install -r requirements.txt`
3. Fill in .env file with your credentials, copy `.env.example` and name it to `.env`, or just rename it outright
4. Run the bot at `main.py`